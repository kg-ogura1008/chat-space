.main_content__body__text{ "data-id": "#{message.id}"}
  %ul
    %li.main_content__body__text__name
      = message.user.name
    %li.main_content__body__text__date
      = message.created_at
  - if message.text.present?
    %p
      = message.text
  = image_tag message.image.url if message.image.present?
